[project]
name = "shared-workflows-python-smoke"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
workflow-fixture = "shared_workflows_python_smoke:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
